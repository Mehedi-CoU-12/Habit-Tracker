<?xml version="1.0" encoding="utf-8"?>
<!-- Small: one habit's ring. Colours are set from the payload at draw time,
     so the static values here only matter for the launcher's preview. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/small_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="12dp"
    android:background="@drawable/widget_bg">

    <ImageView
        android:id="@+id/small_ring"
        android:layout_width="66dp"
        android:layout_height="66dp"
        android:contentDescription="@string/widget_ring_description" />

    <TextView
        android:id="@+id/small_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="13sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/small_detail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="11sp" />
</LinearLayout>
