# Local API (uncomment when running the API locally)
# NEXT_PUBLIC_API_URL=http://localhost:3333
NEXT_PUBLIC_API_URL=your production-api-url-here

# Must match the API's APP_CLIENT_KEY so requests pass its client check.
NEXT_PUBLIC_APP_CLIENT_KEY=
